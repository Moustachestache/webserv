#lolo
server[0.0.0.0:9292]
{
	SERVER_NAME = localhost
	
	ROOT = www/

	MAX_HEADER_SIZE = 819256

	ROUTE[/]{
		METHOD = GET POST DELETE
		DEFAULT =  
		ALLOW_UPLOAD = allow
		UPLOAD_PATH = upload/

		#REDIRECTION = static_website/
		CGI[bash]
		{
			PATH = /bin/sh
			EXTENSION = .sh .bash}
	}

	ROUTE[/]{
		METHOD = GET POST DELETE
		DEFAULT =
		ALLOW_UPLOAD = allow
		UPLOAD_PATH = upload/
		REDIRECTION = static_website/website/very/far/away/
		CGI[bash]
		{
			PATH = /bin/sh
			EXTENSION = .sh .bash}
	}

	ERROR_STATUS[]{100 = PATH_TO_FILE
	}
}